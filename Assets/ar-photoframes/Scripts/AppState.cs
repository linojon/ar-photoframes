/// <summary>
/// These are the states that the app can be in. This is a simple enum that I placed in a separate script.
/// </summary>
public enum AppState { SCANNING, VIEW, MODIFY, FRAME, IMAGE, POSITION, RESIZE, REMOVE }


